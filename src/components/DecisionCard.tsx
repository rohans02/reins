// One card per verdict. 3px left border carries the entire semantic colour system:
// green = ALLOW, red = BLOCK, amber = ESCALATE. Nothing else on screen is coloured,
// so the red card is the only red thing when it appears. Phase 3.
export function DecisionCard() { return null }
