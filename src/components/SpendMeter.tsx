// ★ The most important component. Segmented progress bar, tabular-nums so digits
// do not jitter as it counts, 300ms ease-out fill on each ALLOW. Phase 3.
export function SpendMeter() { return null }
