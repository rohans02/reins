// A LOG, not a chat UI. Tool calls render mono: > search_catalog({ category: "groceries" }).
// Streams over SSE, so it never needs a spinner. Phase 3.
export function AgentTranscript() { return null }
