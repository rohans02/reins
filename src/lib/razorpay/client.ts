/**
 * Razorpay Node SDK singleton. TEST MODE ONLY.
 * Reads RAZORPAY_KEY_ID / RAZORPAY_KEY_SECRET. Never import this from client components.
 * PHASE 2
 */
export function razorpayClient(): unknown {
  throw new Error('razorpayClient(): not implemented — Phase 2')
}
