/**
 * Mandate Studio — turn a sentence into a signed, scoped authorization.
 * Flow: intent textarea (+3 example chips) -> Draft -> editable review card -> Sign & Activate.
 * Include a collapsed <details> showing raw canonical JSON + signature: judges look for it.
 * Phase 3.
 */
export default function NewMandatePage() {
  return <main className="p-8">Mandate Studio — Phase 3</main>
}
